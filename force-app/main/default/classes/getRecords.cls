public with sharing class getRecords {
    @AuraEnabled
    public static string methodName(string objname,List<Map<String,String>> fields){
        try {
            String []array='';
            for()
            List<sObject> objLst = [SELECT ]
        } catch (Exception e) {
            throw new AuraHandledException(e.getMessage());
        }
    }
}